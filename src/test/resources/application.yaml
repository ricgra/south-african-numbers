spring:
  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:phonenumberstestdb
    username: user
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
      show-sql: true